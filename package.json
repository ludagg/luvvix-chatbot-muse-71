{
  "name": "vite_react_shadcn_ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "lint": "eslint .",
    "preview": "vite preview",
    "cap:init": "npx cap init --web-dir=dist",
    "cap:android": "npx cap add android",
    "postbuild": "npm run cap:android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@emoji-mart/data": "^1.2.1",
    "@emoji-mart/react": "^1.1.1",
    "@hookform/resolvers": "^3.9.0",
    "@radix-ui/react-accordion": "^1.2.0",
    // ... (toutes vos dépendances existantes conservées)
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@eslint/js": "^9.9.0",
    // ... (toutes vos devDependencies existantes)
    "vite": "^5.4.1"
  },
  "capacitor": {
    "webDir": "dist"
  }
}